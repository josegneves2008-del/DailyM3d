PR de suporte para fix/responsive-images

Este PR contém pequenas mudanças de suporte para permitir abertura do PR (arquivo de metadata).

Mudanças principais a revisar neste PR:
- Carrossel adicionado no `index.html` usando imagens em `images/carrocel`.
- Capturas inseridas em `images/capturas` nos cartões de preview.
- Estilos e script de carrossel adicionados (`styles.css`, `carousel.js`).

Tarefas posteriores:
- Gerar `optimized/` com múltiplas larguras e WebP.
- Substituir screenshots por `<picture>` + `srcset`.
